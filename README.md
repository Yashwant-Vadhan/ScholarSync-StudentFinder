# ScholarSync-StudentFinder 🎓

**ScholarSync-StudentFinder** is a web-based tool designed to help teachers and students manage classroom workflows with ease.  
It allows flexible student entry, bulk upload via CSV/Excel, defaulter tracking, and export options — all in a clean, user-friendly interface.

---

## ✨ Features
- ➕ **Add Students Manually** — Enter register number and name directly.
- 📂 **Bulk Upload** — Upload student lists via CSV or Excel (supports flexible headers like `Roll` or `Register Number`).
- 📋 **Student Table** — View all students with completion status and remove option.
- ✅ **Defaulter Finder**  
  - By completion status (who hasn’t finished work).  
  - By specific register numbers (comma-separated input).
- 📤 **Download Options**  
  - Export **complete class list** as Excel.  
  - Export **defaulter list** as Excel.  
  - (PDF export can be added if needed).
- 🔒 **Persistent Data** — Student list is saved in browser local storage.
- 🎨 **Responsive UI** — Built with Bootstrap for a clean, modern look.

---

## 🛠️ Tech Stack
- **HTML5, CSS3, JavaScript**
- **Bootstrap 5** (UI styling)
- **PapaParse** (CSV parsing)
- **SheetJS (xlsx)** (Excel parsing and export)
- **jsPDF** (PDF export support)

---

## 🚀 Getting Started
1. Clone the repository:
   ```bash
   git clone https://github.com/YashwantVadhan/ScholarSync-StudentFinder.git
